{
  "name": "formula",
  "version": "3.9.0",
  "description": "Formula compiler and function library",
  "main": "index.js",
  "types": "index.d.js",
  "scripts": {
    "all": "npm run build && npm test",
    "test": "babel-tape-runner test/*.js | faucet",
    "docs": "groc src/*.js -o ./docs/code; groc test/*.js -o ./docs/test",
    "clean": "rm -rf lib; mkdir lib;",
    "build": "BABEL_ENV=production make && npm run build:umd && npm run build:min && npm run docs",
    "build:parser": "jison src/parser.jison -m commonjs -o src/parser.js",
    "build:umd": "rollup -f=umd -n=Formula src/functions.js > lib/formula.esm; babel lib/formula.esm > lib/formula.js",
    "build:min": "uglifyjs lib/formula.js > lib/formula.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FormBucket/formula.git"
  },
  "keywords": [
    "Spreadsheets",
    "JavaScript"
  ],
  "author": "FormBucket LLC",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/FormBucket/formula/issues"
  },
  "homepage": "https://github.com/FormBucket/formula#readme",
  "devDependencies": {
    "@babel/cli": "^7.2.0",
    "@babel/core": "^7.2.0",
    "@babel/preset-env": "^7.2.0",
    "babel-tape-runner": "^3.0.0",
    "docco": "^0.7.0",
    "eslint": "^1.8.0",
    "faucet": "0.0.1",
    "groc": "^0.8.0",
    "jison": "^0.4.18",
    "rollup": "^0.26.2",
    "tape": "^4.5.1",
    "uglify-js": "^3.3.16"
  }
}
